// Alpine.js is managed entirely by Livewire — do NOT import or start it here.
// Importing Alpine separately causes "multiple instances" conflict that breaks wire:model.live.
// Livewire v3 bundles and boots Alpine automatically.
